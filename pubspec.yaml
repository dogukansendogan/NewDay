name: new_day
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # UI ve Araçlar
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.0.3
  go_router: ^14.0.0
  google_fonts: ^6.3.2
  flutter_animate: ^4.5.2
  intl: ^0.19.0
  uuid: ^4.4.0
  confetti: ^0.7.0
  
  # FIREBASE (Uyumlu Sürümler)
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  google_sign_in: 6.2.1  # Şapkasız, sabit sürüm
  flutter_staggered_grid_view: ^0.7.0
  image_picker: ^1.2.1

# PATRON MODU: Bu kısım, sistemi 6.2.1 kullanmaya ZORLAR
dependency_overrides:
  google_sign_in: 6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true